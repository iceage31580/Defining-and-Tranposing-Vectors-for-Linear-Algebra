import numpy as np

# Element-wise Multiplication 
def dot_product(v1, v2):
  return sum(x*y for x,y in zip(v1,v2)
  
# Using numpy.multiply() code to translate the mathematical definition of a dot product of vectors v1  and v2
def dot_product_numpy(v1, v2):
  return np.dot(v1, v2)
  
#Vector-wise Multiplication
# numpy.multiply code to translate the mathematical definition of a dot product of vectors v1 and v2
def dot_product_numpy(v1, v2)
  return np.sum(np.multiply(v1, v2))

# Using numpy library numpy.matmul() code to translate the mathematical definition of a dot products of vectors
# v1 and v2 
def dot_product_numpy(v1, v2)
  return np.matmul(v1, v2)
 
 # Using numpy library numpy.dot() 
 def d0t_product_numpy(v1, v2):
  return np.dot(v1, v2)
